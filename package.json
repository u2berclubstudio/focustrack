{
  "name": "focustrack",
  "version": "1.0.0",
  "description": "Multi-tenant focus-session tracker with distraction logging, business dashboards and a master admin panel",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "seed": "node seed.js",
    "test": "node test.js && node test-security.js && node test-alerts.js && node test-migration.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "nodemailer": "^6.9.14"
  },
  "optionalDependencies": {
    "better-sqlite3": "^11.5.0"
  }
}
